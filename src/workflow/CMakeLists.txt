set(plass_workflow_source_files
        workflow/Assembler.cpp
        PARENT_SCOPE
        )

set(penguin_workflow_source_files
        workflow/Nuclassembler.cpp
        workflow/GuidedNuclassembler.cpp
        PARENT_SCOPE
        )

